from tkinter import *
from PIL import Image, ImageTk
from datetime import *
import time

class Dashboard:
    def __init__(self, window):
        self.window = window

        # header
        # self.header = Frame(self.window, bg='#FFFFFF')
        # self.header.place(x = 300, y = 0, width = 1070, height = 60)

        # self.text = Label(self.window, text="Traffic Capture at Ho Chi Minh City", font=("Roboto", 25, "bold"), fg='blue', activebackground='white')
        # self.text.place(x = 320, y = 40)

        # # sidebar
        # self.sidebar = Frame(self.window, bg ='#FFFFFF00')
        # self.sidebar.place(x = 0, y = 0, width = 300, height = 950)


